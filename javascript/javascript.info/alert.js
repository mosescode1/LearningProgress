'use strict';

// alert("I'm javascript but linked in using the src");

// declaring twice with let causes an Error

let message = 'Hello';
console.log(message);

/*
This would cause an Error 
we dont re-declarre with let
let message = "World"; 

So we can declare a variable once with let then assign it 
let nextMessage;
nextMessage = "World";
*/
